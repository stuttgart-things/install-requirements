---
# defaults file for install-requirements
state: present
python_version: "{{ 3 if ansible_python.version.major == 3 else 2 }}"
pip_package_name: "{{ python3-pip if ansible_python.version.major == 3 else python-pip }}"